# Config Package

`@ainyc/aeo-platform-config` owns typed environment parsing for the platform services. Phase 1 keeps it intentionally small so the API and worker share the same placeholder runtime contract from day one.
