# Contracts Package

`@ainyc/aeo-platform-contracts` defines shared DTOs, enums, config-schema validation (Zod), and structured error codes for the platform.

Key exports:
- Project, run, keyword, competitor DTOs
- `canonry.yaml` config-schema validation
- Structured error codes and types
- Quota-related shared types
